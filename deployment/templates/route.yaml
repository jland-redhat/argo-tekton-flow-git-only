kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: {{ .Values.appName }}
spec:
  to:
    kind: Service
    name: {{ .Values.appName }}
    weight: 100
  port:
    targetPort: 8080
  wildcardPolicy: None
  tls:
    termination: edge